<style>
    /* 1. Modern Reset & Variables */
    :root {
        --accent: #23d5ab; /* Modern Emerald */
        --bg-dark: #0f172a; /* Deep Slate */
        --card-bg: rgba(30, 41, 59, 0.7);
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Inter', system-ui, -apple-system, sans-serif;
    }

    body {
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: radial-gradient(circle at top left, #1e293b, #0f172a);
        color: #f8fafc;
        padding: 20px;
    }

    /* 2. The Bento Card Design */
    .asset-card {
        background: var(--card-bg);
        backdrop-filter: blur(24px);
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 32px;
        width: 100%;
        max-width: 500px;
        padding: 48px;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        transition: transform 0.4s ease;
    }

    .asset-card:hover {
        border-color: rgba(35, 213, 171, 0.3);
    }

    header {
        text-align: center;
        margin-bottom: 40px;
    }

    header h2 {
        font-size: 2.25rem;
        font-weight: 800;
        letter-spacing: -1px;
        background: linear-gradient(to right, #fff, #94a3b8);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    textarea {
            width: 100%;
            padding: 16px 20px;
            background: rgba(15, 23, 42, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            color: white;
            font-size: 1rem;
            transition: all 0.2s ease;
            resize: none; /* Keeps the UI clean */
            min-height: 100px;
        }
    
        textarea:focus {
            outline: none;
            border-color: var(--accent);
            background: rgba(15, 23, 42, 0.8);
        }
    
        /* Specific Grid for Money fields */
        .money-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 16px;
        }
    
        /* Icon Hint for Location */
        .location-wrapper {
            position: relative;
        }
        
        .location-wrapper input {
            padding-left: 45px;
        }
    
        .location-wrapper::before {
            content: '📍';
            position: absolute;
            left: 18px;
            top: 48px; /* Adjusted for label space */
            font-size: 1.2rem;
            z-index: 5;
        }

    /* 3. Sleek Input Fields */
    .field-wrapper {
        position: relative;
        margin-bottom: 24px;
    }

    .field-wrapper label {
        display: block;
        font-size: 0.75rem;
        font-weight: 600;
        color: var(--accent);
        text-transform: uppercase;
        letter-spacing: 1.5px;
        margin-bottom: 8px;
        margin-left: 4px;
    }

    input, select {
        width: 100%;
        padding: 16px 20px;
        background: rgba(15, 23, 42, 0.6);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 16px;
        color: white;
        font-size: 1rem;
        transition: all 0.2s ease;
    }

    input:focus, select:focus {
        outline: none;
        border-color: var(--accent);
        background: rgba(15, 23, 42, 0.8);
        box-shadow: 0 0 0 4px rgba(35, 213, 171, 0.1);
    }

    /* 4. Sub-category Grid */
    .grid-row {
        display: grid;
        grid-template-columns: 1.2fr 1fr;
        gap: 16px;
    }

    /* 5. Custom File Upload (Modern Style) */
    .file-input-wrapper {
        border: 2px dashed rgba(255, 255, 255, 0.1);
        border-radius: 16px;
        padding: 20px;
        text-align: center;
        transition: 0.3s;
        cursor: pointer;
    }

    .file-input-wrapper:hover {
        border-color: var(--accent);
        background: rgba(35, 213, 171, 0.05);
    }

    input[type="file"] {
        background: transparent;
        border: none;
        padding: 0;
        font-size: 0.85rem;
    }

    /* 6. High-Contrast Button */
    .btn-glow {
        width: 100%;
        padding: 18px;
        margin-top: 32px;
        background: var(--accent);
        color: #0f172a;
        border: none;
        border-radius: 16px;
        font-size: 1.1rem;
        font-weight: 700;
        cursor: pointer;
        transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }

    .btn-glow:hover {
        transform: translateY(-4px);
        box-shadow: 0 15px 30px rgba(35, 213, 171, 0.4);
        filter: brightness(1.1);
    }
</style>
<div class="asset-card">
    <header>
        <h2>New Listing</h2>
        <p style="color: #64748b; font-size: 0.9rem;">Turn your assets into passive income</p>
    </header>

    <form action="/add-asset" method="POST" enctype="multipart/form-data">
        <div class="field-wrapper">
            <label>Item Title</label>
            <input type="text" name="Name" placeholder="What are you renting?" required>
        </div>

        <div class="field-wrapper">
            <label>Description</label>
            <textarea name="Description" placeholder="Describe the condition, usage, and any rules..."></textarea>
        </div>

        <div class="grid-row">
            <div class="field-wrapper">
                <label>Category</label>
                <select name="Category">
                    <option value="Tools">Tools</option>
                    <option value="Electronics">Electronics</option>
                    <option value="Vehicles">Vehicles</option>
                    <option value="Camping">Camping</option>
                </select>
            </div>
            <div class="field-wrapper">
                <label>Sub-Category</label>
                <input type="text" name="SubCategory" placeholder="e.g. DSLR Camera">
            </div>
        </div>

        <div class="money-grid">
            <div class="field-wrapper">
                <label>Daily Rent</label>
                <input type="number" name="Price" placeholder="₹ 0.00" required>
            </div>
            <div class="field-wrapper">
                <label>Security Deposit</label>
                <input type="number" name="Deposit" placeholder="₹ Refundable">
            </div>
        </div>

        <div class="field-wrapper location-wrapper">
            <label>Pickup Location</label>
            <input type="text" name="Location" placeholder="City or Neighborhood" required>
        </div>

        <div class="field-wrapper">
            <label>Item Photos</label>
            <div class="file-input-wrapper">
                <input type="file" name="Image" accept="image/*" required>
            </div>
        </div>

        <button type="submit" class="btn-glow">Launch Listing</button>
    </form>
</div>

      
    
     