<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Owner Dashboard | RENTANY</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        /* 1. Page Setup */
        :root {
            --accent-primary: #6366f1; /* Indigo */
            --accent-success: #059669; /* Emerald */
            --accent-danger: #ef4444;  /* Rose */
            --text-main: #0f172a;      /* Slate 900 */
            --text-muted: #64748b;     /* Slate 500 */
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            min-height: 100vh;
            background: radial-gradient(circle at top left, #1e293b, #0f172a);
            padding: 40px 20px;
        }

        .main-wrapper {
            max-width: 900px;
            margin: auto;
        }

        h1.dashboard-title {
            color: white;
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 40px;
            letter-spacing: -1px;
        }

        /* 2. The Request Card */
        .request-card {
            background: rgba(255, 255, 255, 0.98);
            width: 100%;
            border-radius: 24px;
            display: flex;
            padding: 32px;
            margin-bottom: 24px;
            align-items: center;
            justify-content: space-between;
            position: relative; /* Context for the absolute Edit button */
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .request-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        }

        /* 3. Information Section (Left) */
        .info-section {
            flex: 1;
        }

        .info-section .badge-new {
            display: inline-block;
            font-size: 0.65rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1.2px;
            color: var(--accent-primary);
            margin-bottom: 8px;
        }

        .info-section h3 {
            color: var(--text-main);
            font-size: 1.35rem;
            font-weight: 700;
            margin-bottom: 8px;
        }

        .info-section p {
            color: var(--text-muted);
            font-size: 0.9rem;
            margin-bottom: 4px;
        }

        .earnings-pill {
            display: inline-flex;
            align-items: center;
            background: #f0fdf4;
            color: var(--accent-success);
            padding: 6px 16px;
            border-radius: 50px;
            font-size: 0.9rem;
            font-weight: 700;
            margin-top: 12px;
            border: 1px solid rgba(5, 150, 105, 0.1);
        }

        /* 4. Actions Section (Right) */
        .action-group {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .btn-approve {
            background: var(--text-main);
            color: white;
            padding: 14px 28px;
            border-radius: 14px;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.95rem;
            transition: 0.2s;
            box-shadow: 0 4px 12px rgba(15, 23, 42, 0.2);
        }

        .btn-approve:hover {
            background: #334155;
            transform: scale(1.03);
        }

        .btn-reject {
            color: var(--accent-danger);
            text-decoration: none;
            font-weight: 600;
            font-size: 0.95rem;
            padding: 14px;
            transition: 0.2s;
        }

        .btn-reject:hover {
            opacity: 0.7;
        }

        .status-final {
            font-weight: 800;
            color: var(--text-main);
            text-transform: uppercase;
            font-size: 0.8rem;
            letter-spacing: 1.5px;
            background: #f1f5f9;
            padding: 10px 20px;
            border-radius: 10px;
        }

        /* 5. The Fixed Edit Button */
        .edit-item-btn {
            position: absolute;
            top: 24px;
            right: 32px;
            background: transparent;
            color: var(--accent-primary);
            text-decoration: none;
            font-size: 0.7rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1px;
            padding: 6px 12px;
            border: 1px solid rgba(99, 102, 241, 0.2);
            border-radius: 8px;
            transition: 0.3s;
        }

        .edit-item-btn:hover {
            background: var(--accent-primary);
            color: white;
            border-color: var(--accent-primary);
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 60px;
            color: var(--text-muted);
            background: rgba(255, 255, 255, 0.05);
            border-radius: 24px;
            border: 2px dashed rgba(255, 255, 255, 0.1);
        }
    </style>
</head>
<body>

<div class="main-wrapper">
    <h1 class="dashboard-title">Owner Dashboard</h1>

    {{#each requests}}
    <div class="request-card">
        <a href="/edit-product/{{this.item}}" class="edit-item-btn">Manage Asset</a>

        <div class="info-section">
            <span class="badge-new">Rental Request</span>
            <h3>{{this.itemDetails.Name}}</h3>
            <p>Customer: {{this.userDetails.Name}}</p>
            <p>Date: {{this.date}}</p>
            <div class="earnings-pill">₹{{this.price}} Potential Earnings</div>
        </div>

        <div class="action-group">
            {{#if (eq this.status 'pending')}}
                <a href="/reject-request/{{this._id}}" class="btn-reject">Decline</a>
                <a href="/approve-request/{{this._id}}" class="btn-approve">Approve Request</a>
            {{else}}
                <div class="status-final">{{this.status}}</div>
            {{/if}}
        </div>
    </div>
    {{else}}
    <div class="empty-state">
        <p>No pending requests at the moment.</p>
    </div>
    {{/each}}
</div>

</body>
</html>