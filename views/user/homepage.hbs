<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home | Circular Community</title>
    <style>
        /* 1. Base Setup & Animated Background */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            min-height: 100vh;
            background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            color: white;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* 2. Glassmorphism Navbar */
        nav {
            width: 100%;
            padding: 1rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            letter-spacing: 1px;
        }

        .nav-links {
            display: flex;
            align-items: center;
            gap: 2rem;
        }

        .nav-links a {
            color: white;
            text-decoration: none;
            font-size: 0.9rem;
            text-transform: uppercase;
            transition: 0.3s;
        }

        .nav-links a:hover {
            opacity: 0.7;
        }
        .btn-add {
            background: rgba(255, 255, 255, 0.2);
            padding: 8px 20px;
            border-radius: 50px;
            border: 1px solid rgba(255, 255, 255, 0.4);
            font-weight: bold;
            font-size: 0.8rem !important;
        }
        .btn-add:hover {
            background: white !important;
            color: #e73c7e !important;
            opacity: 1 !important;
        }

        /* 3. Account Profile Section */

        .user-nav-group {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            margin-left: 10px;
        }
        
        .user-name {
            font-size: 0.9rem;
            font-weight: 700;
            color: white;
        }
        
        .nav-sub-links {
            display: flex;
            gap: 12px;
        }
        
        .nav-sub-links a {
            font-size: 0.7rem !important;
            text-transform: uppercase;
            letter-spacing: 1px;
            opacity: 0.7;
            text-decoration: none;
            color: white;
            transition: 0.3s;
        }
        
        .nav-sub-links a:hover {
            opacity: 1;
            color: #23d5ab; /* That nice green color you used */
        }
        
        .logout-text {
            color: #ef4444 !important; /* Red for logout */
        }
        .user-profile {
            display: flex;
            align-items: center;
            gap: 10px;
            background: rgba(255, 255, 255, 0.2);
            padding: 8px 15px;
            border-radius: 50px;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .user-icon {
            width: 30px;
            height: 30px;
            background: #fff;
            color: #e73c7e;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            font-weight: bold;
        }

        /* 4. Main Content */
        .hero {
            height: 80vh; /* Reduced height so users see there is more content below */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .hero h1 {
            font-size: 4rem;
            margin-bottom: 1rem;
            animation: fadeInDown 1s ease;
        }

        .hero p {
            font-size: 1.2rem;
            margin-bottom: 2rem;
            max-width: 600px;
            opacity: 0.9;
        }

        .btn-main {
            padding: 15px 40px;
            background: white;
            color: #23a6d5;
            text-decoration: none;
            border-radius: 50px;
            font-weight: bold;
            transition: 0.3s;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }  

        .btn-main:hover {
            transform: scale(1.05);
            background: #f0f0f0;
        }

        .hero-btns {
            display: flex;
            gap: 20px;
        }
        
        .btn-secondary {
            padding: 15px 40px;
            background: transparent;
            color: white;
            text-decoration: none;
            border-radius: 50px;
            border: 2px solid white;
            font-weight: bold;
            transition: 0.3s;
        }
        
        .btn-secondary:hover {
            background: white;
            color: #23a6d5;
        }

        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .asset-section {
            padding: 80px 5%;
            background: rgba(0, 0, 0, 0.1); /* Slight darken to make cards pop */
        }
        .section-title {
            font-size: 2.5rem;
            margin-bottom: 50px;
            text-shadow: 2px 2px 10px rgba(0,0,0,0.2);
        }
        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 30px;
            max-width: 1200px;
            margin: 0 auto;
        }
        .product-card {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(12px);
            border-radius: 25px;
            border: 1px solid rgba(255, 255, 255, 0.3);
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        
        .product-card:hover {
            transform: translateY(-12px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.3);
            border-color: rgba(255, 255, 255, 0.6);
        }
        
        /* Image Handling */
        .img-container {
            position: relative;
            height: 220px;
            width: 100%;
        }
        
        .product-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        .badge {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(0, 0, 0, 0.6);
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        /* Text Info */
        .product-info {
            padding: 25px;
        }
        
        .product-info h3 {
            font-size: 1.4rem;
            margin-bottom: 10px;
            color: #fff;
        }
        
        .price-tag {
            display: flex;
            align-items: baseline;
            gap: 2px;
            margin-bottom: 20px;
        }
        
        .currency { font-size: 1rem; color: #23d5ab; }
        .amount { font-size: 1.8rem; font-weight: 800; color: #fff; }
        .per-day { font-size: 0.9rem; opacity: 0.8; }
        
        .btn-rent {
            display: block;
            width: 100%;
            padding: 12px;
            background: linear-gradient(90deg, #23a6d5, #23d5ab);
            color: white;
            text-decoration: none;
            border-radius: 12px;
            font-weight: bold;
            text-align: center;
            transition: 0.3s;
        }
        
        .btn-rent:hover {
            filter: brightness(1.1);
            letter-spacing: 1px;
        }

    </style>
</head>
<body>

    <nav>
        <div class="logo">RENTANY</div>
        <div class="nav-links">
            <a href="/">Home</a>
            <a href="/products">Products</a>
            <a href="/add-asset" class="btn-add">+ List Item</a>
            
            <div class="user-profile">
                <div class="user-icon">
                    {{#if user}}
                     {{substring user.Name 0 1}}
                    {{else}}
                        ?
                    {{/if}}
                </div>
                <div class="user-nav-group">
                        {{#if user}}
                            <span class="user-name">{{user.Name}}</span>
                            <div class="nav-sub-links">
                                <a href="/my-rentals">My Rentals</a>
                                <a href="/owner-dashboard">Lender Hub</a>
                                <a href="/logout" class="logout-text">Logout</a>
                            </div>
                        {{else}}
                            <a href="/login" class="login-btn">Login</a>
                        {{/if}}
            </div>
        </div>
    </nav><main class="hero">
        <h1>Welcome, {{#if user}}{{user.Name}}{{else}}to the Community{{/if}}</h1>
        <p>Borrow what you need, lend what you don't. Build a better circle.</p>
        
        <div class="hero-btns">
            <a href="#explore" class="btn-main">Browse Tools</a>
            {{#if user}}
                <a href="/add-asset" class="btn-secondary">List Your Gear</a>
            {{else}}
                <a href="/signup" class="btn-secondary">Join to Earn</a>
            {{/if}}
        </div>
    </main>
    
    <section id="explore" class="asset-section">
        <h2 class="section-title">Available in Your Community</h2>
        <div class="product-grid">
            {{#each assets}}
            <div class="product-card">
                <div class="img-container">
                    <img src="/asset-images/{{this._id}}.jpg" alt="{{this.Name}}" class="product-img">
                    <span class="badge">{{this.Category}}</span>
                </div>
                
                <div class="product-info">
                    <h3>{{this.Name}}</h3>
                    <div class="price-tag">
                        <span class="currency">₹</span>
                        <span class="amount">{{this.Price}}</span>
                        <span class="per-day">/day</span>
                    </div>
                    <a href="/product-details/{{this._id}}" class="btn-rent">Rent Now</a>
                </div>
            </div>
            {{/each}}
        </div>
    </section>

</body>
</html>